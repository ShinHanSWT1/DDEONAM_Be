server:
  port: 8081

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: 3600000

spring:
  application:
    name: EcoDrive

  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: ${KAKAO_CLIENT_ID}
            client-secret: ${KAKAO_CLIENT_SECRET}
            client-authentication-method: client_secret_post
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/api/login/oauth2/code/{registrationId}"
            scope:
              - profile_nickname
              - profile_image
              - account_email
            client-name: Kakao
        provider:
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id

  flyway:
    enabled: true

  main:
    banner-mode: console

logging:
  level:
    org.springframework.security.web: trace
    com.gorani.ecodrive: debug
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

app:
  frontend-url: ${FRONTEND_URL}
  cdn-url: https://${AWS_BUCKET_NAME}.s3.${AWS_REGION}.amazonaws.com
  driving-dummy:
    base-dir: ${DRIVING_DUMMY_BASE_DIR:generated/driving}
    refresh-cron: ${DRIVING_DUMMY_REFRESH_CRON:0 0 * * * *}
    generation-cron: ${DRIVING_DUMMY_GENERATION_CRON:0 0 6 * * *}
    generation-enabled: ${DRIVING_DUMMY_GENERATION_ENABLED:true}
    python-command: ${DRIVING_DUMMY_PYTHON_COMMAND:python3}
    script-path: ${DRIVING_DUMMY_SCRIPT_PATH:scripts/generate_driving_dummy_json.py}
    default-sessions-per-user: ${DRIVING_DUMMY_DEFAULT_SESSIONS_PER_USER:2}
    extra-session-probability: ${DRIVING_DUMMY_EXTRA_SESSION_PROBABILITY:0.2}
    target-users: ${DRIVING_DUMMY_TARGET_USERS:1:1}

cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY}
      secret-key: ${AWS_SECRET_KEY}
    region:
      static: ap-northeast-2
    s3:
      bucket: ${AWS_BUCKET_NAME}
